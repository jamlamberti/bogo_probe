language: python
python:
    - "2.7"

before_install:
    - pip install pytest pytest-cov codecov

install:
    - pip install -r requirements.txt

script:
    - py.test --cov=.

after_success:
    - codecov
